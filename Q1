def stringCleaner(arg1):
  ''' Removes non-alphabetical letters from the input and returns the word in lowercase letters
  
  Args:
    arg1: a string filled with words and numbers lowercase and uppercase

  Return:
    String cleaned into becoming only lowercase alphabetical letters
  '''
  answer = ''
  for c in x:
    if c.isalpha():
      answer += c
  return answer.lower()

word = input("Enter any word/number combination: ")

print(f"The word {word} is cleaned to become {stringCleaner(word)}")
